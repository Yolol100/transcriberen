{
  "schema_version": "3.0",
  "capability_id": "public-youtube-channel-caption-corpus",
  "repository": "Yolol100/transcriberen",
  "evidence_role": "controlled_runtime_public_youtube_channel_caption_corpus",
  "requires_account": false,
  "requires_api_key": false,
  "requires_mcp": false,
  "runtime_target": "self-hosted-or-local-direct-network",
  "inputs": ["youtube-channel-url", "optional-language"],
  "fixed_policy": {"year": 2026, "max_videos": 1000, "comments_per_video": 7, "comment_sort": "top", "include_replies": false},
  "outputs": ["result.json", "manifest.json", "progress.json", "processed-index.json", "videos/<video_id>/transcript.txt when captions exist", "videos/<video_id>/comments.json", "videos/<video_id>/metadata.json", "channel-corpus.zip", "SHA256SUMS.txt"],
  "tools": [
    {"id": "yt-dlp", "version": "2026.09.16.232951", "sha256": "f8ca14db511702a5dbfc5a527056312907ddd0914d0b4036f108d6849e17ef61", "release_channel": "nightly"},
    {"id": "deno-ejs-runtime", "version": "2.9.7", "sha256": "c6527f24f4b16031d3ae4fa9f658d5f11534c8d84ce7dc8502420280919c3490"}
  ],
  "boundaries": [
    "Only public YouTube channel URLs are accepted; they are normalized to the /videos tab.",
    "Direct video, Short and playlist requests are rejected.",
    "Only videos whose exact upload_date is in 2026 enter the corpus.",
    "At most 1000 channel video entries are inspected per run.",
    "Per matched video, at most 7 top-level comments are requested with YouTube-side top sorting; replies are excluded with yt-dlp max-depth=1 and runtime filtering.",
    "Unavailable comments do not invalidate a valid transcript, but they make the channel corpus partial rather than complete.",
    "Video metadata that cannot prove the upload year is retained as unresolved evidence and makes the run partial.",
    "processed-index.json is scoped to the current run and never exports unrelated persistent-cache history.",
    "The same immutable trusted runtime commit is used for request resolution and self-hosted execution.",
    "No cookies, login, proxy, CAPTCHA bypass, PO token or media download is used.",
    "The persistent host cache stores validated caption text, not project truth; comments are refreshed per run.",
    "Project identity, Skill routing and promotion of transcript knowledge are owned outside this repository."
  ]
}
