{
  "schema_version": "2.0",
  "owner_skill": "webactueel-workflow",
  "owner_mode": "transcribeer",
  "project_id": "project-transcriberen",
  "repository": "Yolol100/transcriberen",
  "source_set_version": "2.2.0-captions-only",
  "evidence_role": "controlled_runtime_public_youtube_caption_acquisition",
  "requires_account": false,
  "requires_api_key": false,
  "requires_mcp": false,
  "runtime_target": "self-hosted-or-local-direct-network",
  "inputs": ["youtube-video-url", "youtube-short-url", "optional-language"],
  "outputs": ["result.json", "transcript.txt when captions exist", "SHA256SUMS.txt"],
  "tools": [
    {
      "id": "yt-dlp",
      "version": "2026.08.20.234504",
      "release_channel": "nightly",
      "provisioning": "dedicated-runner prerequisite",
      "purpose": "discover and download one public caption track without media"
    },
    {
      "id": "deno-ejs-runtime",
      "version": "2.9.5",
      "provisioning": "dedicated-runner prerequisite",
      "purpose": "JavaScript challenge runtime used by yt-dlp"
    }
  ],
  "boundaries": [
    "Only direct public YouTube video and Short URLs are accepted.",
    "Exactly one real caption track is selected; English then Dutch then another language, with manual before automatic within a language.",
    "If no caption track exists, the request succeeds with status skipped_no_captions and no transcript file.",
    "The acquisition job runs only on a dedicated self-hosted Linux x64 runner or through scripts/run_local.sh on a normal direct network connection.",
    "No comments, channel or playlist discovery, search, ranking, topic filtering, engagement data, knowledge handoff, article/feed/sitemap extraction, audio fallback or Whisper exists in this runtime.",
    "No cookies, login, proxy, CAPTCHA bypass, PO token or media download is used.",
    "Only minimal caption provenance is persisted; raw yt-dlp metadata is not persisted.",
    "All output is controlled_runtime evidence."
  ]
}
